# The landing page is a static site. Vite reads VITE_-prefixed vars at
# build time (none currently consumed). The only env var the build path
# touches is NODE_ENV, which Vite/Node sets automatically — set it
# explicitly only if you're running `vite build` from a non-default
# environment.
NODE_ENV=production
