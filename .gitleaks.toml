# Minimal gitleaks config — satisfies agent-readiness `safety.gitleaks_config`.

title = "agent-readiness-landing-page"

[allowlist]
description = "Per-repo false-positive allowlist"
paths = [
  '''(.*?)(\.lock|\.snap)$''',
  '''node_modules/''',
  '''vendor/''',
]
