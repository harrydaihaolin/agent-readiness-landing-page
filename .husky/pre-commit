#!/usr/bin/env bash
# Husky-style pre-commit hook. Runs lint + tests on staged files.
# To enable: `npm install --save-dev husky && npx husky install`.
set -euo pipefail

if command -v npm >/dev/null 2>&1; then
  npm run lint --silent
  npm test --silent
fi
